#include "main.h"
#include <string.h>
/**
 * _strcat- concantanate two string
 * @dest: pointer for string
 * @src: pointer for string
 * Return  : char*
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
