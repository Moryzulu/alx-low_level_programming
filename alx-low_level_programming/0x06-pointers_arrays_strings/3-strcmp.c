#include "main.h"
#include <string.h>
/**
 * _strcmp - compare two pointers using strcmp
 * @s1 : pointer with string
 * @s2 : ponter with string
 * Return: results of comparison
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
